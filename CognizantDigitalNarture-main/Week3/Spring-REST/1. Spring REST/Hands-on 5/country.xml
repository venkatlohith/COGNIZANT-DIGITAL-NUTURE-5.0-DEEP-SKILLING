<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
        https://www.springframework.org/schema/beans/spring-beans.xsd">

    <!--
        STEP 1: Singleton scope (default) - constructor called ONCE
        Both country and anotherCountry point to the SAME instance
    -->
    <bean id="country" class="com.cognizant.springlearn.Country">
        <property name="code" value="IN" />
        <property name="name" value="India" />
    </bean>

    <!--
        STEP 2: Prototype scope - constructor called TWICE
        Each call to getBean() creates a NEW instance
        Uncomment below and comment the above bean to test prototype scope
    -->
    <!--
    <bean id="country" class="com.cognizant.springlearn.Country" scope="prototype">
        <property name="code" value="IN" />
        <property name="name" value="India" />
    </bean>
    -->

</beans>
