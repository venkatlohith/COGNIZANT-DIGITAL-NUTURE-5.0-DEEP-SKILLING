spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          # issuer-uri: URL of the Authorization Server that issued the JWT
          # Spring auto-fetches the public keys from issuer-uri/.well-known/openid-configuration
          # to validate incoming JWT tokens
          issuer-uri: https://issuer.example.com
