# Centralized config for product-service, served by config-server
product:
  low-stock-threshold: 10
  default-currency: USD

management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh
