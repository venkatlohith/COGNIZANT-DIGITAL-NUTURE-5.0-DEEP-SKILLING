server:
  port: 8761

spring:
  application:
    name: eureka-server

eureka:
  client:
    # This node is a server, not a client -> don't register with itself
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false
